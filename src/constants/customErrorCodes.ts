let customErrorCodes: {
  PERMISSION_DENIED_TO_RESOURCE: string;
  RESOURCE_ALREADY_EXIST: string;
  RESOURCE_NOT_FOUND: string;
  INVALID_RATING_TYPE: string;
} = {
  PERMISSION_DENIED_TO_RESOURCE: "PERMISSION_DENIED_TO_RESOURCE",
  RESOURCE_ALREADY_EXIST: "RESOURCE_ALREADY_EXIST",
  RESOURCE_NOT_FOUND: "RESOURCE_NOT_FOUND",
  INVALID_RATING_TYPE: "INVALID_RATING_TYPE",
};

export default customErrorCodes;
